services:
  proxy:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      - PORT=${PORT:-3000}
      - ACCOUNTS_FILE=/app/data/antigravity-accounts.json
    volumes:
      - ./data:/app/data:z
    restart: unless-stopped
